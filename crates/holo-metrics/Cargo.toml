[package]
name        = "holo-metrics"
version     = "0.1.0"
edition     = "2021"
description = "v9-G1 falsification witnesses — R7 evidence for L-DPC24 §5 (holo-falsif-witnesses)"
authors     = ["Vasilev Dmitrii <admin@t27.ai>"]
license     = "Apache-2.0"

[dependencies]
serde      = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
